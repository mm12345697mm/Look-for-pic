[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "sh -c 'gunicorn -b 0.0.0.0:${PORT:-8080} -w 2 --timeout 600 server:app'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
